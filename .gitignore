node_modules/
*.log
result
# archived prints (originals + rendered) — user data
print-archive/
# fetched via scripts/fetch-profile.sh — redistribution terms unclear
profiles/*.icc
# APK signing key, auto-generated on first build — private, never commit
android/keystore.jks
# gradle build output
android/.gradle/
android/app/build/
